# Normalize line endings so the repo behaves the same on every platform.
* text=auto eol=lf

# Windows-only helper script keeps CRLF.
*.ps1 text eol=crlf

# Binary assets.
*.png binary
*.crx binary
*.zip binary
